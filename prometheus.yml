global:
  scrape_interval: 5s # Scrape targets every 5 seconds
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'quantum_ev_engine'
    static_configs:
      # Targets the 'engine' service from your docker-compose on port 8000
      - targets: ['engine:8000']
